import 'package:flutter/material.dart';

class TechCategoryView extends StatelessWidget {
  const TechCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("techCategory")));
  }
}
