import 'package:flutter/material.dart';

class MusicCategoryView extends StatelessWidget {
  const MusicCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("musicCategory")));
  }
}
