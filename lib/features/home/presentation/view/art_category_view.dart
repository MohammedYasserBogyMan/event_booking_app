import 'package:flutter/material.dart';

class ArtCategoryView extends StatelessWidget {
  const ArtCategoryView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("ArtCategory")));
  }
}
